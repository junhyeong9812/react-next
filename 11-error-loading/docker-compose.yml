services:
  backend:
    build: ../backend
    ports:
      - "8080:8080"
    environment:
      API_KEY: demo-secret-key

  react:
    build: ./react
    ports:
      - "3111:3000"
    depends_on:
      - backend
    environment:
      VITE_API_BASE: http://localhost:8080

  next:
    build: ./next
    ports:
      - "3112:3000"
    depends_on:
      - backend
    environment:
      API_BASE: http://backend:8080
